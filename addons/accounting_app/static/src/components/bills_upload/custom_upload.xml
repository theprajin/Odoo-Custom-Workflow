<?xml version="1.0" encoding="UTF-8"?>

<templates>
    <t t-name="accounting_app.DropZone" owl="1">
        <div t-if="props.visible"
            t-attf-class="o_drop_area"
            t-on-dragover.prevent="()=>{}"
            t-on-dragleave="props.hideZone"
            t-on-drop.prevent="onDrop">
            <i class="fa fa-upload fa-10x"></i>
        </div>
    </t>

    <t t-name="accounting_app.AccountingFileUploader" owl="1">
        <div t-att-class="props.record and props.record.data ? 'oe_kanban_color_' + props.record.data.color : ''">
            <FileUploader
                acceptedFileExtensions="props.acceptedFileExtensions"
                fileUploadClass="'account_file_uploader'"
                multiUpload="true"
                onUploaded.bind="onFileUploaded"
                onUploadComplete.bind="onUploadComplete">
                <t t-set-slot="toggler">
                    <t t-if="props.togglerTemplate" t-call="{{ props.togglerTemplate }}"/>
                    <t t-else="" t-slot="default"/>
                </t>
                <t t-slot="extra"/>
            </FileUploader>
        </div>
    </t>

    <t t-name="accounting_app.ListRenderer" t-inherit="web.ListRenderer" t-inherit-mode="primary" owl="1">
        <xpath expr="//div[@t-ref='root']" position="before">
            <AccountDropZone
                visible="state.dropzoneVisible"
                hideZone="() => state.dropzoneVisible = false"/>
        </xpath>
        <xpath expr="//div[@t-ref='root']" position="attributes">
            <attribute name="t-on-dragenter.stop.prevent">() => state.dropzoneVisible = true</attribute>
        </xpath>
    </t>

    <t t-name="accounting_app.KanbanRenderer" t-inherit="web.KanbanRenderer" t-inherit-mode="primary" owl="1">
        <xpath expr="//div[@t-ref='root']" position="before">
            <AccountDropZone
                visible="state.dropzoneVisible"
                hideZone="() => state.dropzoneVisible = false"/>
        </xpath>
        <xpath expr="//div[@t-ref='root']" position="attributes">
            <attribute name="t-on-dragenter.stop.prevent">() => state.dropzoneVisible = true</attribute>
        </xpath>
    </t>

    <t t-name="accounting_app.ListView.Buttons" t-inherit="web.ListView.Buttons" t-inherit-mode="primary" owl="1">
        <xpath expr="//*[@class='btn btn-primary o_list_button_add']" position="after">
            <t t-call="account.AccountViewUploadButton"/>
        </xpath>
    </t>

    <t t-name="accounting_app.KanbanView.Buttons" t-inherit="web.KanbanView.Buttons" t-inherit-mode="primary" owl="1">
        <xpath expr="//*[@class='btn btn-primary o-kanban-button-new']" position="after">
            <t t-call="account.AccountViewUploadButton"/>
        </xpath>
    </t>

    <t t-name="accounting_app.AccountViewUploadButton" owl="1">
        <!-- No record is available so rely on the action context to contain the default_move_type -->
        <AccountingFileUploader>
            <t t-set-slot="default">
                <button type="button" class="btn btn-secondary o_button_upload_bill">
                    Upload
                </button>
            </t>
        </AccountingFileUploader>
    </t>

    <t t-name="accounting_app.DashboardKanbanRecord" owl="1">
        <div
            role="article"
            t-att-class="getRecordClasses()"
            t-att-data-id="props.canResequence and props.record.id"
            t-att-tabindex="props.record.model.useSampleModel ? -1 : 0"
            t-on-click="onGlobalClick"
            t-on-dragenter.stop.prevent="() => state.dropzoneVisible = true"
            t-ref="root">
            <AccountingFileUploader record="props.record">
                <t t-set-slot="extra">
                    <AccountDropZone
                        visible="state.dropzoneVisible"
                        hideZone="() => state.dropzoneVisible = false"/>
                    <t t-call="{{ templates['kanban-box'] }}"/>
                </t>
            </AccountingFileUploader>
        </div>
    </t>

    <t t-name="accounting_app.JournalUploadLink" owl="1">
        <div t-att-class="props.btnClass" groups="account.group_account_invoice">
            <a href="#" t-out="props.linkText"/>
        </div>
    </t>

    <t t-name="accounting_app.ListView.Buttons" t-inherit="web.ListView.Buttons" t-inherit-mode="primary" owl="1">
        <xpath expr="//*[@class='btn btn-primary o_list_button_add']" position="after">
            <t t-call="accounting_app.AccountingJsonUploader"/>
        </xpath>
    </t>
    
    <t t-name="accounting_app.AccountingJsonUploader" owl="1"  >
        <!-- t-import="['accounting_app.AccountJsonUploader']" -->
        <div class="o_account_file_uploader">
                
                    <t t-set-slot="default">
                        <button type="button" class="btn btn-secondary o_button_upload_bill" t-on-click="onJsonClickButton">
                            Click Here
                        </button>
                    </t>        
                
                <!-- <textarea t-model="state.value" rows="10" cols="50" placeholder="Paste your JSON data here"></textarea>
                <button type="button" t-on-click="onJsonDataSubmit" class="btn btn-primary">
                    <t t-esc="props.linkText || 'Upload JSON Data'"/>
                </button> -->        
        </div>
    </t>

</templates>
